# P2-T13 — Lab-to-production portability probe

- `new-design` was exercised with `folder-parity-probe`.
- The probe found and fixed JSX-in-`.ts` output and a config/manifest metadata mismatch.
- The corrected folder built in the Lab, copied unchanged into production, passed production discovery and full Next build, and was removed from both checkouts.
