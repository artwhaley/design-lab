# P2-T02 — discovery and manifest parity

- Production-shaped manifest discovery and asset materialization are active.
- `npm run design:check` passes with only `obsidian` installed after probe cleanup.
- New Design scaffolding runs discovery automatically.
