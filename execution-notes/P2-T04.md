# P2-T04 — route and surface truth

- Removed the obsolete singular member surface from the production-shaped catalog and required slot list.
- Member profile deep links are external; the directory remains `members` and the management workspace remains `management.person`.
- Legacy contract-probe compatibility remains isolated to legacy tests.
