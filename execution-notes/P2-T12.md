# P2-T12 — cross-host render evidence

- `test:cross-host` compares the production Obsidian oracle route with the Lab's
  deterministic `production-preview` fixture profile across all 16 Class A
  surfaces at desktop `1440×1000`, compact `1024×900`, and mobile `390×844`.
- The completed packet contains 48 route/viewport pairs in
  `docs/parity/parity-review/manifest.json` and matching production/Lab/diff
  PNGs, canonicalized DOM captures, and semantic input JSON.
- The final run passed with `0` failures, `0.000%` maximum changed pixels,
  zero normalized DOM mismatches, zero asset mismatches, and zero semantic
  input mismatches. Both hosts used the same Chromium executable, locale,
  UTC timezone, device scale factor, and bundled asset hashes.
- DOM parity is exact after the narrowly documented runtime-noise
  canonicalization in the manifest. Authored text, classes, styles, values,
  navigation, and content remain compared; this is not a text-masked visual
  escape hatch.
- The production oracle is deterministic and uses the same production fixture
  models as the Lab. The obsolete incubation-golden Playwright spec was retired
  so the named visual gate cannot silently compare against pre-production
  source.
- P2-GATE human review remains required for the packet's Class A surfaces,
  shared editor/tool, and Site Studio preview.
