# P2-T10 — iframe render environment

- Vite preview uses the production asset namespace, font faces, and global preview reset.
- Iframe sizing is explicit and independently scrollable; desktop and phone presets drive real source viewports.
- Font loading and shell-boundary browser checks pass.
