# P2-T07 — action and API emulation

- Added sanctioned form and `/api/*` emulation for folders, departments, roles, invitations, claims, and document-type actions.
- Unknown endpoints and unsupported methods report preview errors rather than silently succeeding.
- Compare panes share backend snapshots and mutation results.
