# P2-T09 — Studio, config, and assets

- Obsidian config defaults, migration, validation, resolved CSS variables, thumbnail, atmosphere, and bundled fonts are used directly from the production folder.
- Studio bank persistence and saved config behavior pass the Lab suite.
